using System;


// PascalCase-->> First letter of each word is capitalized (FirstName, LastName, etc.)
// camelCase-->> First letter of the first word is lowercase, but subsequent words start with uppercase (productPrice, category, etc.)
class Test
{
    
    public static void Main(string[] args)
    {
       string firstName="sony";
       string lastName="zaman";
       double gpa=1.1;
       bool isRegistered=true;

       double productPrice=29.99;
       string category="smart";
       bool isAvailable=true;
       int productSold=150;
       char bloodGroup='A';

       Console.WriteLine(firstName);
       Console.WriteLine(lastName);
       Console.WriteLine(gpa);
       console.WriteLine(isRegistered);
    
       Console.WriteLine(productPrice);
       Console.WriteLine(category);
       Console.WriteLine(isAvailable);
       Console.WriteLine(productSold);
       console.WriteLine(bloodGroup);


    }
}